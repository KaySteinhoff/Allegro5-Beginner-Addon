The input system is in its first stage.

The Input check is a boolean check for the event of a button pressed with further specification.

Example:

if(key[KS_AL_ADDON_KEY_DOWN])
{
  if(key[KS_AL_ADDON_KEY_A])
  {
    animHappy.Show(NULL);
  }
}
else
{
  animIdle.Show(NULL);
}
